#include <bits/stdc++.h>

using namespace std;

int main()
{
	string a, b;
	cin >> a >> b;
	int count = 0;
	int l = 0;
	int asize = a.size();
	int extras = 0;
	reverse(b.begin(), b.end());
	cout << b << endl;
	cout << count;
}
